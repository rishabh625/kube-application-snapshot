---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: volumebackups.backup.infracloud-citadel-vbakup.com
spec:
  group: backup.infracloud-citadel-vbakup.com
  names:
    kind: VolumeBackup
    listKind: VolumeBackupList
    plural: volumebackups
    singular: volumebackup
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VolumeBackup is the Schema for the volumebackups API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VolumeBackupSpec defines the desired state of VolumeBackup.
            properties:
              pvcName:
                type: string
              pvcNamespace:
                description: The namespace where the PVC to backup resides.
                type: string
              snapshotName:
                type: string
              volumeSnapshotClassName:
                description: The name of the VolumeSnapshotClass to use for the backup.
                type: string
            required:
            - pvcName
            - pvcNamespace
            - snapshotName
            - volumeSnapshotClassName
            type: object
          status:
            description: VolumeBackupStatus defines the observed state of VolumeBackup.
            properties:
              completionTime:
                description: The time the backup operation was completed.
                format: date-time
                type: string
              message:
                description: A human-readable message indicating details about the
                  last transition.
                type: string
              phase:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                  Represents the current phase of the backup operation.
                type: string
              readyToUse:
                type: boolean
              snapshotHandle:
                description: The name of the VolumeSnapshot object created by this
                  backup.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
